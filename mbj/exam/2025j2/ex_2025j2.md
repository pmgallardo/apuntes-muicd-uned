# EX.2025J2

## EX.2025J2.1

### Enunciado EX.2025J2.1

Dado un modelo $M$ con parámetros $\theta$ y observaciones (datos) $y$, la distribución predictiva a posteriori de nuevas observaciones $\tilde y$ es

a) $p(\tilde y) = p(\tilde y \mid \theta, y)$

b) $p(\tilde y) = \int p(\tilde y, \theta \mid y) \, d\theta$

c) $p(\tilde y) = \int p(\tilde y \mid \theta, y) \, d\theta$

### Solución EX.2025J2.1

c)

---

## EX.2025J2.2

### Enunciado EX.2025J2.2

La probabilidad conjunta $p(u, v, w)$

a) se puede factorizar sólo como $p(u \mid v, w) \cdot p(v \mid w) \cdot p(w)$

b) se puede factorizar de las dos formas anteriores (y más)

c) se puede factorizar sólo como $p(w \mid v, u) \cdot p(v \mid u) \cdot p(u)$

### Solución EX.2025J2.2

b)

---

## EX.2025J2.3

### Enunciado EX.2025J2.3

La utilización de un prior impropio

a) inevitablemente conduce a una distribución a posteriori impropia

b) inevitablemente conduce a una distribución a posteriori propia

c) ninguna de las otras dos respuestas es correcta

### Solución EX.2025J2.3

c)

---

## EX.2025J2.4

### Enunciado EX.2025J2.4

En el análisis de varianza tradicional, si tenemos datos de varios grupos que creemos generados a partir de distribuciones normales (todas ellas caracterizadas por la misma desviación estándar) y el cociente de la media de la suma de mínimos cuadrados entre grupos (Mean Square Between groups) dividido por la misma media pero interna de cada grupo (Mean Square Within groups) es muy superior a 1, ¿cuál de las siguientes afirmaciones es correcta?

a) dicho análisis de varianza indica que no hay evidencia de que existan diferencias entre las medias muestrales de cada grupo y se obtiene una mejor estimación de la media global con todas las observaciones independientemente del grupo al que pertenezcan

b) Las dos afirmaciones anteriores son compatibles.

c) dicho análisis de varianza indica que los grupos tienen medias muestrales muy diferentes y es preferible estimar cada media únicamente con los datos del grupo correspondiente

### Solución EX.2025J2.4

c)

---

## EX.2025J2.5

### Enunciado EX.2025J2.5

El estudio de la calibración de un parámetro $\theta$ en el contexto Bayesiano analiza

a) el cociente entre los valores esperados a priori y a posteriori.

b) diferencias sistemáticas entre el valor esperado a priori (el valor esperado para la distribución de probabilidad a priori) y el valor esperado de la distribución marginal a posteriori

c) diferencias sistemáticas entre el valor esperado de ese parámetro dados los datos ($\hat\theta = E(\theta \mid y)$) y el valor esperado en inferencia si el valor verdadero fuese precisamente $\theta$ (es decir, $p(\hat\theta \mid \theta)$)

### Solución EX.2025J2.5

c)

---

## EX.2025J2.6

### Enunciado EX.2025J2.6

En el caso de que no dispongamos de expresiones analíticas para las distribuciones a posteriori de los parámetros $\theta$ de un modelo (o que el número de parámetros sea tan elevado que no sea posible marginalizar), podemos estimar la exactitud predictiva ($predictive\ accuracy$) de un modelo recurriendo a

a) sustituir la distribución a posteriori por el producto de la verosimilitud y la distribución marginal a posteriori.

b) la estimación

$$
\sum_{i=1}^{n} \log \left( \frac{1}{S} \sum_{s=1}^{S} p(y_i \mid \theta^s) \right)
$$

basada en un conjunto de $S$ muestras de dicha distribución a posteriori.

c) nada; no se puede calcular ni estimar o aproximar.

### Solución EX.2025J2.6

b)

---

## EX.2025J2.7

### Enunciado EX.2025J2.7

El Criterio de Información de Akaike (AIC) se define como

a) -2 veces el logaritmo de la densidad predictiva evaluada en la moda de la distribución a posteriori de los parámetros con una corrección por sobreajuste igual al doble del número de parámetros

b) -2 veces el logaritmo de la densidad predictiva evaluada en la estimación máximo verosímil con una corrección por sobreajuste igual al doble del número de parámetros

c) -2 veces el logaritmo de la densidad predictiva evaluada en la moda de la distribución a posteriori de los parámetros con una corrección por sobreajuste igual al número de parámetros

### Solución EX.2025J2.7

b)

---

## EX.2025J2.8

### Enunciado EX.2025J2.8

El Criterio de Información Bayesiano (BIC) se caracteriza por

a) una penalización por sobreajuste independiente de la cantidad de datos utilizados en el ajuste del modelo

b) tener como objetivo evaluar la exactitud de la densidad predictiva de un modelo

c) estar motivado por el objetivo de estimar la distribución de probabilidad marginal de los datos $p(y)$

### Solución EX.2025J2.8

c)

---

## EX.2025J2.9

### Enunciado EX.2025J2.9

Para poder generar muestras de la distribución a posteriori de los parámetros de un modelo mediante un muestreador de Gibbs es necesario

a) disponer de las verosimilitudes marginales

b) disponer de las distribuciones a posteriori condicionales de un subconjunto de parámetros dados todos los demás (y los datos)

c) que las cadenas de Markov tengan correlaciones elevadas

### Solución EX.2025J2.9

b)

---

## EX.2025J2.10

### Enunciado EX.2025J2.10

¿Cuál de las afirmaciones siguientes es falsa?

a) La aceptación de una propuesta en el algoritmo de Metropolis depende de la generación de un número aleatorio y es por tanto contingente.

b) Si una propuesta es rechazada en el algoritmo de Metropolis, se añade a la muestra una copia del último valor de la cadena.

c) La distribución propuesta de un algoritmo de Metropolis-Hastings tiene que ser simétrica

### Solución EX.2025J2.10

c)
